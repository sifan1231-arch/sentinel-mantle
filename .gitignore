# dependencies
node_modules/
**/node_modules/

# env / secrets
.env
.env.*
!.env.example
**/.env
**/.env.local

# hardhat / solidity
contracts/artifacts/
contracts/cache/
contracts/typechain-types/
contracts/coverage/
contracts/coverage.json

# deployment artifacts regenerated on deploy (local test + ABIs)
contracts/deployments/*.localhost.json
contracts/deployments/abis.json
deployments/*.local.json

# video render output (upload the MP4 separately; don't bloat git)
video/out/
video/.remotion/

# agent runtime
agent/.state/
agent/dist/
agent/*.log

# next.js / web
web/.next/
web/out/
web/dist/
web/.vercel/

# misc
.DS_Store
*.tsbuildinfo
npm-debug.log*
